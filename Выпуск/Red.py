import discord
from discord.ext import commands
import random


intents = discord.Intents.default()
bot = commands.Bot(command_prefix='$', intents=intents)

@bot.event
async def on_ready():
    print(f'Бот {bot.user.name} готов к работе!')

@bot.command(name='info')
async def info(ctx):
    info_text = (
        "Глобальное потепление - это долгосрочное повышение средней температуры "
        "нашей планеты из-за увеличения парниковых газов в атмосфере. "
        "Вот что вы можете сделать, чтобы помочь:\n"
        "1. Сократите потребление энергии.\n"
        "2. Используйте общественный транспорт или ходите пешком, когда это возможно.\n"
        "3. Сократите использование пластика.\n"
        "4. Поддерживайте инициативы по озеленению."
    )
    await ctx.send(info_text)

@bot.command(name='tips')
async def tips(ctx):
    """Выдает советы по борьбе с глобальным потеплением"""
    tips_text = (
        "Вот несколько советов:\n"
        "1. Установите энергоэффективные лампочки.\n"
        "2. Сократите водопользование.\n"
        "3. Перерабатывайте и следите за тем, что вы выбрасываете.\n"
        "4. Участвуйте в местных экологических акциях."
    )
    await ctx.send(tips_text)

@bot.command(name='resources')
async def resources(ctx):
    """Выдает ссылки на ресурсы по глобальному потеплению"""
    resources_text = (
        "Вот некоторые полезные ресурсы:\n"
        "1. [Программа ООН по окружающей среде (UNEP)](https://www.unep.org/)\n"
        "2. [Всемирный фонд дикой природы (WWF)](https://www.worldwildlife.org/)\n"
        "3. [Greenpeace](https://www.greenpeace.org/)\n"
        "4. [Межправительственная группа экспертов по изменению климата (IPCC)](https://www.ipcc.ch/)"
    )
    await ctx.send(resources_text)

@bot.command(name='fact')
async def fact(ctx):
    facts = [
        "Факт: В среднем глобальная температура поднялась на примерно 1.2°C с конца 19 века.",
        "Факт: Основной причиной глобального потепления является сжигание ископаемого топлива и вырубка лесов.",
        "Факт: Уровень океана продолжает расти из-за таяния ледников на полюсах.",
        "Факт: В 2020 году мир испытывал больше тепловых волн, чем когда-либо ранее за всю историю наблюдений."
    ]
    await ctx.send(random.choice(facts))

@bot.command(name='eco_practice')
async def eco_practice(ctx):
    """Выдает случайную экологически чистую практику."""
    practices = [
        "Сокращение потребления мяса в пользу растительных продуктов.",
        "Использование многоразовых сумок вместо пластиковых.",
        "Сбор дождевой воды для полива растений.",
        "Замиращение электроприборов, когда они не используются."
    ]
    await ctx.send(random.choice(practices))

@bot.command(name='endangered_species')
async def endangered_species(ctx):
    """Информация о животных, находящихся под угрозой исчезновения из-за изменения климата."""
    species_info = [
        "Полярные медведи: их ареалы обитания сокращаются из-за таяния льдов.",
        "Тигры: изменение климата влияет на привычную среду обитания и добычу.",
        "Попугаи: изменение климата и потеря лесов угрожают их существованию.",
        "Черепахи: они сталкиваются с угрозами из-за повышения уровня моря и изменения температуры."
    ]
    await ctx.send(random.choice(species_info)) 



bot.run("")